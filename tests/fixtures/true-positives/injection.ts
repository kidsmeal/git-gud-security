// True-positive fixtures for injection patterns

const result1 = eval(userInput);
const fn = new Function(body);
const child_process = require("child_process");
child_process.exec(command);
const out = execSync(cmd);
const shell = exec(`ls ${userDir}`);

const query = `SELECT * FROM users WHERE id = ${userId}`;
const insert = "INSERT INTO orders SET name = '" + name + "'";

element.dangerouslySetInnerHTML = { __html: userContent };
document.getElementById("output").innerHTML = content;

const token = jwtDecode(rawToken);
const decoded = jwt_decode(rawToken);
const payload = jwt.decode(rawToken, { verify: false });
const alg = { algorithm: "none" };

const tpl = render_template_string(userTemplate);
