# Demo App

A fun little project. Fully serverless, no backend needed — it talks to Supabase
directly from the frontend.

## Setup

Just add your service role key to .env and you're done. No login required, the API
is open by default so anyone can try it.

Deploy anywhere: 100% client-side, host on any static CDN.
